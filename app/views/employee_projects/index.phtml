<?php require_once __DIR__ . '/../shared/header.phtml'; ?>

<div class="container mx-auto px-4 py-8">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold"><?= $title ?></h1>
        <div class="flex space-x-2">
            <a href="<?= route('employees.show', ['id' => $employee->id]) ?>" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                Back to Employee
            </a>
            <a href="<?= route('employees.index') ?>" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                All Employees
            </a>
        </div>
    </div>

    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <h2 class="text-xl font-bold mb-4">Employee Information</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
            <div>
                <h3 class="text-gray-700 font-bold">Name</h3>
                <p class="text-gray-600"><?= htmlspecialchars($employee->name) ?></p>
            </div>
            <div>
                <h3 class="text-gray-700 font-bold">Email</h3>
                <p class="text-gray-600"><?= htmlspecialchars($employee->email) ?></p>
            </div>
        </div>
    </div>
    
    <!-- Projects List -->
    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <h2 class="text-xl font-bold mb-4">Assigned Projects</h2>
        
        <?php if (empty($projects)) : ?>
            <p class="text-gray-500">This employee is not assigned to any projects yet.</p>
        <?php else : ?>
            <div class="overflow-x-auto">
                <table class="min-w-full bg-white">
                    <thead>
                        <tr>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Project Name</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Status</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Role in Project</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($projects as $project) : ?>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="py-3 px-4"><?= htmlspecialchars($project->name) ?></td>
                                <td class="py-3 px-4">
                                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                                        <?= $project->status === 'In Progress' ? 'bg-blue-100 text-blue-800' : 
                                            ($project->status === 'Completed' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800') ?>">
                                        <?= htmlspecialchars($project->status) ?>
                                    </span>
                                </td>
                                <td class="py-3 px-4"><?= htmlspecialchars($project->pivot->role ?? 'Not specified') ?></td>
                                <td class="py-3 px-4">
                                    <div class="flex space-x-2">
                                        <a href="<?= route('projects.show', ['id' => $project->id]) ?>" class="text-blue-600 hover:text-blue-900">View Project</a>
                                        <form action="<?= route('employee-projects.remove') ?>" method="post" class="inline">
                                            <input type="hidden" name="project_id" value="<?= $project->id ?>">
                                            <input type="hidden" name="employee_id" value="<?= $employee->id ?>">
                                            <button type="submit" class="text-red-600 hover:text-red-900">Remove</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        <?php endif; ?>
    </div>
    
    <!-- Assign to New Project -->
    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <h2 class="text-xl font-bold mb-4">Assign to Project</h2>
        
        <form action="<?= route('employee-projects.assign') ?>" method="post">
            <input type="hidden" name="employee_id" value="<?= $employee->id ?>">
            
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2" for="project_id">
                    Project
                </label>
                <select class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                    id="project_id" name="project_id" required>
                    <option value="">Select Project</option>
                    <?php 
                    // Get all projects that this employee is not already assigned to
                    $assignedProjectIds = array_map(function($p) { return $p->id; }, $projects);
                    $allProjects = \App\Models\Project::all();
                    foreach ($allProjects as $availableProject) : 
                        if (!in_array($availableProject->id, $assignedProjectIds)) :
                    ?>
                        <option value="<?= $availableProject->id ?>"><?= htmlspecialchars($availableProject->name) ?></option>
                    <?php 
                        endif;
                    endforeach; 
                    ?>
                </select>
            </div>
            
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2" for="role">
                    Project Role
                </label>
                <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
                    id="role" type="text" name="role" placeholder="Developer, Designer, Manager, etc." required>
            </div>
            
            <div class="flex items-center justify-between">
                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">
                    Assign to Project
                </button>
            </div>
        </form>
    </div>
</div>

<?php require_once __DIR__ . '/../shared/footer.phtml'; ?>
