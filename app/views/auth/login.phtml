<div class="flex items-center justify-center min-h-[70vh]">
  <div class="max-w-md w-full mx-4">
    <!-- Glass Effect Card -->
    <div class="glass-effect p-8 rounded-2xl shadow-xl">
      <div class="mb-8 text-center">
        <div
          class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-600 to-purple-600 rounded-xl mb-4 shadow-lg">
          <i class="fas fa-user-lock text-white text-xl"></i>
        </div>
        <h2 class="text-3xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent mb-2">
          Acesso ao Sistema</h2>
        <p class="text-gray-600">Entre com suas credenciais</p>
      </div>

      <form action="<?= route('auth.login.post') ?>" method="POST" class="space-y-5">
        <input type="hidden" name="_token" value="<?= csrf_token() ?>">

        <div>
          <label for="email" class="block text-gray-700 text-sm font-semibold mb-2">Endereço de Email</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
              <i class="fas fa-envelope text-gray-400"></i>
            </div>
            <input type="email" id="email" name="email" required
              class="w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200">
          </div>
        </div>

        <div>
          <label for="password" class="block text-gray-700 text-sm font-semibold mb-2">Senha</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
              <i class="fas fa-lock text-gray-400"></i>
            </div>
            <input type="password" id="password" name="password" required
              class="w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200">
          </div>
        </div>

        <div>
          <button type="submit"
            class="w-full bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-bold py-3 px-4 rounded-lg transition duration-300 transform hover:scale-[1.02] shadow-lg">
            <i class="fas fa-sign-in-alt mr-2"></i> Entrar
          </button>
        </div>
      </form>

      <!-- Test Users Info -->
      <div class="mt-8 p-4 bg-blue-50/80 rounded-lg border border-blue-100">
        <p class="text-xs font-semibold text-blue-800 mb-2">🧪 Contas para teste:</p>
        <div class="gap-2 text-xs flex flex-col">
          <div class="bg-white/60 p-2 rounded border border-blue-100">
            <p class="font-medium text-blue-700">Admin</p>
            <p class="text-gray-600">klesio@admin.com</p>
            <p class="text-gray-500">123456</p>
          </div>
          <div class="bg-white/60 p-2 rounded border border-blue-100">
            <p class="font-medium text-blue-700">RH</p>
            <p class="text-gray-600">caio@rh.com</p>
            <p class="text-gray-500">123456</p>
          </div>
          <div class="bg-white/60 p-2 rounded border border-blue-100">
            <p class="font-medium text-blue-700">Usuário</p>
            <p class="text-gray-600">flavio@user.com</p>
            <p class="text-gray-500">123456</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>