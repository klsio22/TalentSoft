<?php if (\Lib\Authentication\Auth::check()): ?>
<nav class="bg-blue-600 shadow-lg">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <a href="<?= route('root') ?>" class="text-white text-xl font-bold">
            <i class="fas fa-briefcase mr-2"></i>TalentSoft
          </a>
        </div>
        <div class="hidden md:ml-6 md:flex md:space-x-8 items-center">
          <a href="<?= route('root') ?>"
            class="text-blue-100 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
            <i class="fas fa-home mr-1"></i> Início
          </a>
          <?php if (\Lib\Authentication\Auth::isAdmin() || \Lib\Authentication\Auth::isHR()): ?>
          <a href="<?= route('employees.index') ?>"
            class="text-blue-100 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
            <i class="fas fa-users mr-1"></i> Funcionários
          </a>
          <?php endif; ?>
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <!-- Link para o perfil -->
        <a href="<?= route('profile.show') ?>"
          class="text-blue-100 hover:text-white px-3 py-2 rounded-md text-sm font-medium flex items-center">
          <i class="fas fa-user mr-1"></i>
          <span class="hidden sm:inline">Meu Perfil</span>
        </a>

        <span class="text-blue-100 text-sm hidden sm:block">
          Olá,
          <?= htmlspecialchars($this->currentUser()->name, ENT_QUOTES, 'UTF-8') ?>
        </span>

        <a href="<?= route('auth.logout') ?>"
          class="bg-blue-500 hover:bg-blue-700 text-white px-3 py-2 rounded-md text-sm font-medium">
          <i class="fas fa-sign-out-alt mr-1"></i> Sair
        </a>
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden flex items-center">
        <button type="button" class="text-blue-100 hover:text-white focus:outline-none focus:text-white"
          id="mobile-menu-button">
          <i class="fas fa-bars text-lg"></i>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="md:hidden hidden" id="mobile-menu">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
      <a href="<?= route('root') ?>"
        class="text-blue-100 hover:text-white block px-3 py-2 rounded-md text-base font-medium">
        <i class="fas fa-home mr-1"></i> Início
      </a>
      <?php if (\Lib\Authentication\Auth::isAdmin() || \Lib\Authentication\Auth::isHR()): ?>
      <a href="<?= route('employees.index') ?>"
        class="text-blue-100 hover:text-white block px-3 py-2 rounded-md text-base font-medium">
        <i class="fas fa-users mr-1"></i> Funcionários
      </a>
      <?php endif; ?>
      <a href="<?= route('profile.show') ?>"
        class="text-blue-100 hover:text-white block px-3 py-2 rounded-md text-base font-medium">
        <i class="fas fa-user mr-1"></i> Meu Perfil
      </a>
      <a href="<?= route('auth.logout') ?>"
        class="text-blue-100 hover:text-white block px-3 py-2 rounded-md text-base font-medium">
        <i class="fas fa-sign-out-alt mr-1"></i> Sair
      </a>
    </div>
  </div>
</nav>

<script>
  // Mobile menu toggle
  document.addEventListener('DOMContentLoaded', function () {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');

    if (mobileMenuButton && mobileMenu) {
      mobileMenuButton.addEventListener('click', function () {
        mobileMenu.classList.toggle('hidden');
      });
    }
  });
</script>
<?php endif; ?>