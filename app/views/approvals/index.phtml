<div class="container mx-auto px-4 py-8">
    <div class="flex items-center mb-4">
        <nav class="text-sm breadcrumbs">
            <ul class="flex space-x-2">
                <?php if (\Lib\Authentication\Auth::isAdmin()): ?>
                <li><a href="<?= route('admin.home') ?>" class="text-blue-600 hover:text-blue-800">Admin Dashboard</a> <span class="mx-2">/</span></li>
                <?php elseif (\Lib\Authentication\Auth::isHR()): ?>
                <li><a href="<?= route('hr.home') ?>" class="text-blue-600 hover:text-blue-800">HR Dashboard</a> <span class="mx-2">/</span></li>
                <?php else: ?>
                <li><a href="<?= route('user.home') ?>" class="text-blue-600 hover:text-blue-800">Dashboard</a> <span class="mx-2">/</span></li>
                <?php endif; ?>
                <li class="text-gray-500">Approval Requests</li>
            </ul>
        </nav>
    </div>
    
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-2xl font-bold"><?= $title ?></h1>
        <div class="flex space-x-2">
            <a href="<?= route('approvals.create') ?>" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                New Approval Request
            </a>
        </div>
    </div>

    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <?php if (empty($approvals)) : ?>
            <p class="text-gray-500">No approval requests found.</p>
        <?php else : ?>
            <div class="overflow-x-auto">
                <table class="min-w-full bg-white">
                    <thead>
                        <tr>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Title</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Project</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Requested By</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Date</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Status</th>
                            <th class="py-3 px-4 bg-gray-100 font-semibold text-sm text-gray-700 text-left">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($approvals as $approval) : ?>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="py-3 px-4"><?= htmlspecialchars($approval->title) ?></td>
                                <td class="py-3 px-4">
                                    <?php 
                                    $project = \App\Models\Project::findById($approval->project_id);
                                    if ($project) : 
                                    ?>
                                        <a href="<?= route('projects.show', ['id' => $project->id]) ?>" class="text-blue-600 hover:text-blue-900">
                                            <?= htmlspecialchars($project->name) ?>
                                        </a>
                                    <?php else : ?>
                                        Unknown Project
                                    <?php endif; ?>
                                </td>
                                <td class="py-3 px-4">
                                    <?php 
                                    $employee = \App\Models\Employee::findById($approval->employee_id);
                                    if ($employee) : 
                                    ?>
                                        <?= htmlspecialchars($employee->name) ?>
                                    <?php else : ?>
                                        Unknown Employee
                                    <?php endif; ?>
                                </td>
                                <td class="py-3 px-4"><?= date('Y-m-d', strtotime($approval->created_at)) ?></td>
                                <td class="py-3 px-4">
                                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                                        <?php 
                                        if ($approval->status === 'Approved') {
                                            echo 'bg-green-100 text-green-800';
                                        } elseif ($approval->status === 'Rejected') {
                                            echo 'bg-red-100 text-red-800';
                                        } else {
                                            echo 'bg-yellow-100 text-yellow-800';
                                        }
                                        ?>">
                                        <?= htmlspecialchars($approval->status) ?>
                                    </span>
                                </td>
                                <td class="py-3 px-4">
                                    <div class="flex space-x-2">
                                        <a href="<?= route('approvals.show', ['id' => $approval->id]) ?>" class="text-blue-600 hover:text-blue-900">View</a>
                                        
                                        <?php if ($approval->status === 'Pending') : ?>
                                            <?php if (\Lib\Authentication\Auth::isAdmin() || \Lib\Authentication\Auth::isHR()) : ?>
                                                <form action="<?= route('approvals.approve') ?>" method="post" class="inline">
                                                    <input type="hidden" name="id" value="<?= $approval->id ?>">
                                                    <button type="submit" class="text-green-600 hover:text-green-900">Approve</button>
                                                </form>
                                                <form action="<?= route('approvals.reject') ?>" method="post" class="inline">
                                                    <input type="hidden" name="id" value="<?= $approval->id ?>">
                                                    <button type="submit" class="text-red-600 hover:text-red-900">Reject</button>
                                                </form>
                                            <?php endif; ?>
                                            
                                            <?php 
                                            // Only the employee who created the request or admin/HR can delete it
                                            $currentEmployee = \Lib\Authentication\Auth::user();
                                            if (($currentEmployee && $currentEmployee->id === $approval->employee_id) || 
                                                \Lib\Authentication\Auth::isAdmin() || \Lib\Authentication\Auth::isHR()) : 
                                            ?>
                                                <form action="<?= route('approvals.destroy') ?>" method="post" class="inline">
                                                    <input type="hidden" name="id" value="<?= $approval->id ?>">
                                                    <button type="submit" class="text-red-600 hover:text-red-900">Delete</button>
                                                </form>
                                            <?php endif; ?>
                                        <?php endif; ?>
                                    </div>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        <?php endif; ?>
    </div>
</div>

